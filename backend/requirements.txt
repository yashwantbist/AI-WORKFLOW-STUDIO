# Web framework
fastapi==0.111.1
uvicorn[standard]==0.30.1

# Data validation
pydantic==2.7.4
pydantic-settings==2.3.4

# HTTP client (for NVIDIA NIM calls — future use)
httpx==0.27.0

# Authentication (future use)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Testing
pytest==8.2.2
httpx==0.27.0          # also used as async test client via TestClient
